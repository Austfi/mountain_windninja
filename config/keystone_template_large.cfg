num_threads = 1

# ======================================================
# KEYSTONE LARGE DOMAIN
# Area: ~12km x 9km (original larger area)
# ======================================================
elevation_file = /home/austin_finnell/keystone_automation/static_data/keystone.tif

initialization_method = wxModelInitialization
wx_model_type = NOMADS-HRRR-CONUS-3-KM

start_year  = {start_year}
start_month = {start_month}
start_day   = {start_day}
start_hour  = {start_hour}
start_minute = {start_minute}

stop_year    = {stop_year}
stop_month   = {stop_month}
stop_day     = {stop_day}
stop_hour    = {stop_hour}
stop_minute  = {stop_minute}

time_zone   = UTC
forecast_duration = {forecast_duration}

# Mesh resolution - 120m for larger domain (~26s/hour)
mesh_resolution = 120.0
units_mesh_resolution = m

# OpenFOAM momentum solver (conservation of mass & momentum)
momentum_flag = true
number_of_iterations = 300

# Default vegetation (uniform). Comment this out if using a Landscape file above.
# vegetation = trees
diurnal_winds = true

output_wind_height = 10.0
units_output_wind_height = m

write_goog_output = true
goog_out_use_consistent_color_scale = true
units_goog_out_resolution = m

# ASCII output for lightweight animations
write_ascii_output = true
ascii_out_resolution = -1
units_ascii_out_resolution = m
